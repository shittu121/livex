import { User, Globe, Camera, ArrowRight, Calendar, Upload } from 'lucide-react'

// Map of icon strings to Lucide React components
export const iconMap: Record<string, React.ElementType> = {
  User,
  Globe,
  Camera,
  ArrowRight,
  Calendar,
  Upload
}